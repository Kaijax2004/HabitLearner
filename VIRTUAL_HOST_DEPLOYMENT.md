# HabitLearner 虚拟主机部署指南

## 🏠 虚拟主机部署步骤

### 第一步：准备文件

1. **构建项目**：
   ```bash
   npm run build
   ```

2. **准备上传文件**：
   - `dist/` 文件夹中的所有文件
   - `.htaccess` 文件（解决SPA路由问题）

### 第二步：通过控制面板上传

#### 方法1：使用文件管理器
1. 登录虚拟主机控制面板
2. 找到"文件管理器"或"文件管理"
3. 进入网站根目录（通常是 `public_html` 或 `www`）
4. 上传 `dist/` 文件夹中的所有文件
5. 上传 `.htaccess` 文件到根目录

#### 方法2：使用FTP客户端
1. 使用FileZilla等FTP工具
2. 连接到您的虚拟主机
3. 上传所有文件到根目录

### 第三步：文件结构检查

上传完成后，您的网站根目录应该包含：

```
public_html/ (或 www/)
├── index.html
├── favicon.ico
├── favicon.svg
├── favicon.png
├── .htaccess          ← 重要！解决404问题
└── assets/
    ├── *.css
    ├── *.js
    └── *.svg
```

## 🔧 关键配置文件说明

### .htaccess 文件作用

这个文件包含解决SPA路由404问题的关键配置：

```apache
# 核心配置：处理SPA路由
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

**工作原理**：
1. 当用户访问 `/auth` 时
2. 检查是否存在 `auth` 文件或目录
3. 如果不存在，重定向到 `index.html`
4. Vue Router接管路由处理

## 🧪 测试步骤

### 1. 基本访问测试
- 访问：`https://habitlearner.cn/`
- 应该显示登录页面

### 2. SPA路由测试
- 访问：`https://habitlearner.cn/auth`
- 应该显示登录页面，而不是404

### 3. 其他路由测试
- 访问：`https://habitlearner.cn/dashboard`
- 应该正常显示（需要先登录）

## 🔍 常见问题解决

### 问题1：仍然显示404错误

**可能原因**：
- `.htaccess` 文件没有上传
- 文件位置不正确
- 虚拟主机不支持URL重写

**解决方案**：
1. 确认 `.htaccess` 文件在根目录
2. 检查文件名是否正确（注意前面的点）
3. 联系虚拟主机提供商确认支持URL重写

### 问题2：静态资源404

**可能原因**：
- `assets/` 文件夹没有上传
- 文件权限问题

**解决方案**：
1. 确认 `assets/` 文件夹完整上传
2. 检查文件权限（通常虚拟主机会自动设置）

### 问题3：页面显示但样式错乱

**可能原因**：
- CSS文件路径错误
- 缓存问题

**解决方案**：
1. 清除浏览器缓存
2. 检查CSS文件是否正确加载

## 📋 虚拟主机部署检查清单

- [ ] 项目已构建（`npm run build`）
- [ ] `dist/` 文件夹内容已上传
- [ ] `.htaccess` 文件已上传到根目录
- [ ] 文件结构正确
- [ ] 测试根路径访问
- [ ] 测试SPA路由访问
- [ ] 测试静态资源访问
- [ ] 清除浏览器缓存测试

## 🎯 不同虚拟主机提供商的注意事项

### 阿里云虚拟主机
- 根目录通常是 `htdocs/`
- 支持 `.htaccess` 配置
- 可能需要手动启用URL重写

### 腾讯云虚拟主机
- 根目录通常是 `wwwroot/`
- 支持 `.htaccess` 配置
- 默认启用URL重写

### 其他提供商
- 根目录可能是 `public_html/`、`www/`、`htdocs/` 等
- 大部分支持 `.htaccess` 配置
- 如有问题请联系客服

## 🚀 部署完成后的优化

### 1. 启用HTTPS
- 在控制面板中申请SSL证书
- 启用强制HTTPS重定向

### 2. 配置CDN
- 如果虚拟主机支持，启用CDN加速
- 可以显著提升访问速度

### 3. 监控和备份
- 定期备份网站文件
- 监控网站访问情况

## 📞 技术支持

如果遇到问题：
1. 检查虚拟主机控制面板的错误日志
2. 联系虚拟主机提供商技术支持
3. 确认虚拟主机支持的功能

---

**重要提醒**：确保 `.htaccess` 文件正确上传，这是解决SPA路由404问题的关键！
