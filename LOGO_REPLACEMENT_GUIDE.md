# Logo 替换指导

## 📁 文件位置

Logo 文件位于：`src/assets/logo.svg`

## 🔄 替换方法

### 方法一：直接替换（推荐）
1. 将你的新 logo 文件重命名为 `logo.svg`
2. 替换 `src/assets/logo.svg` 文件
3. 保存文件，应用会自动更新

### 方法二：使用新文件名
1. 将你的 logo 文件放入 `src/assets/` 目录
2. 修改 `src/components/Logo.vue` 文件中的引用路径
3. 找到第 2 行的 import 语句：
   ```javascript
   import logoSvg from '@/assets/logo.svg'
   ```
4. 将 `logo.svg` 改为你的文件名

## 📋 文件要求

### 格式要求
- **推荐格式**：SVG（矢量图，支持缩放）
- **支持格式**：PNG、JPG（位图，固定尺寸）

### 尺寸要求
- **SVG**：无尺寸限制，建议设计为正方形
- **PNG/JPG**：建议 512x512 像素或更高分辨率

### 设计建议
- **背景**：透明背景（PNG）或无背景（SVG）
- **颜色**：单色设计，支持深色模式
- **风格**：简洁现代，与应用整体风格一致
- **比例**：正方形或接近正方形

## 🎨 深色模式适配

Logo 会自动适配深色模式：
- **浅色模式**：深色 logo 显示在浅色背景上
- **深色模式**：浅色 logo 显示在深色背景上

如果你的 logo 是彩色的，建议：
1. 提供两个版本（深色和浅色）
2. 或者使用单色设计，让系统自动适配

## 🔧 自定义样式

如需调整 logo 大小，修改 `src/components/Logo.vue` 中的尺寸类：

```javascript
// 不同尺寸选项
const sizeClasses = {
  small: 'w-4 h-4',      // 16x16px
  default: 'w-6 h-6',    // 24x24px  
  large: 'w-8 h-8',      // 32x32px
  xl: 'w-12 h-12'        // 48x48px
}
```

## 📱 显示位置

Logo 在以下位置显示：
1. **桌面端侧边栏**：左上角，尺寸 large (48x48px)
2. **移动端顶部**：当标题为 "HabitLearner" 时显示
3. **登录页面**：桌面端和移动端都有显示

## ⚠️ 注意事项

1. **文件大小**：建议不超过 100KB
2. **浏览器兼容性**：SVG 格式兼容性最好
3. **缓存问题**：替换后可能需要清除浏览器缓存
4. **备份**：替换前建议备份原文件

## 🚀 快速替换步骤

1. 准备你的 logo 文件（推荐 SVG 格式）
2. 将文件重命名为 `logo.svg`
3. 复制到 `src/assets/` 目录，替换现有文件
4. 保存文件
5. 刷新浏览器查看效果

## 📞 技术支持

如果遇到问题，请检查：
- 文件格式是否正确
- 文件路径是否正确
- 浏览器控制台是否有错误信息
- 是否需要清除浏览器缓存
